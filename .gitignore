# dependencies and build output
/node_modules
/dist
/.next
/.vinext
/coverage
tsconfig.tsbuildinfo

# local environment
.env*
.DS_Store
*.log

# generated theme packs (build them with `npm run pack:theme -- packs/<name>`)
packs/*.zip

# your own cluster values (route host, editors); see docs/openshift.md
deploy/openshift/overlays/local/
