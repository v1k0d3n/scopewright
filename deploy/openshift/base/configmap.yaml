# scopewright-access exists only as the RBAC target the proxy checks (see rbac.yaml).
apiVersion: v1
kind: ConfigMap
metadata:
  name: scopewright-access
  labels:
    app: scopewright
data:
  purpose: "Users who can 'get' this ConfigMap may open Scopewright."
